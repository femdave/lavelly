import React from "react";
import Navbar from "./Navbar";

const Hero = () => {
  return (
    <section className="flex flex-col bg-dark-bg w-full h-screen">
      <Navbar />
    </section>
  );
};

export default Hero;
